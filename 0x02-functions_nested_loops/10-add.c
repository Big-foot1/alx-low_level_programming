#include "main.h"

/**
 * add - adds two integers
 * Return: summation of the two interger values
 * @a: number we pass to function add
 * @b: number we pass to the function add
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
