#include "main.h"
#include "stdlib.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: number we pass to the _abs function
 * Return: absolute value
 */

int _abs(int n)
{
	int absolute_value;

	absolute_value = abs(n);
	return (absolute_value);
}
